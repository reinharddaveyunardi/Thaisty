import {View, Text} from "react-native";
import React from "react";

export default function Orderid({route}: any) {
    const {orderId} = route.params;
    return (
        <View>
            <Text></Text>
        </View>
    );
}
